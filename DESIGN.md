# 设计方案

## 1. 产品目标

目标用户是第一次接触 Hermes、已经用过通用聊天模型但不熟悉 Agent 运行边界的人。产品不从营销首页开始，而是直接进入课程工作台，让用户在阅读一个概念后立刻做一次可判定练习。

核心学习闭环：

```text
概念说明 -> 观察 Agent 轨迹 -> 在模拟环境决策 -> 即时反馈 -> 记录掌握度 -> 下一课
```

## 2. 信息架构

### 课程工作台

- 左栏：八节课程、完成状态、总进度
- 中栏：本节目标、Agent 回合轨迹、三步引导、实践沙盒、前后课程
- 右栏：掌握度、检查点、即时反馈、进度重置

### 项目研究

- 官方核心、官方实验、社区界面、生态索引、中文指南和示例 Skill
- 每个项目展示核心思路、设计理念、主要特性、技术栈和原始链接
- 筛选器明确区分官方与社区项目

### 架构说明

- 内容层、学习引擎、持久化层、UI Shell
- GitHub/Docs 到课程数据再到反馈状态的单向数据流
- 真实 Hermes 集成的安全边界与后续扩展点

## 3. 功能模块

| 模块 | 职责 | 主要状态 |
|---|---|---|
| `data.js` | 课程、题目、官方文档、GitHub 项目、架构说明 | 静态只读数据 |
| `CourseSidebar` | 课程导航与完成状态 | `activeLesson`, `completed` |
| `CourseView` | 引导步骤、Agent 轨迹、课程前后导航 | `stepIndex` |
| `PracticePanel` | 单选、多选、构建器判定与反馈 | `selection`, `values`, `status` |
| `ProgressRail` | 总掌握度与本节检查点 | 派生状态 |
| `ResearchView` | 项目分析与筛选 | `filter` |
| `ArchitectureView` | 系统设计与技术取舍 | 无本地状态 |
| App persistence | 版本化保存与恢复 | `localStorage v1` |

## 4. 用户交互流程

1. 用户打开应用，直接进入第一课。
2. 阅读目标并观察四段式 Agent 轨迹。
3. 在三步引导中切换概念说明。
4. 在沙盒内选择授权、工具、记忆目标，或构建配置命令。
5. 点击“运行检查”，学习引擎返回成功或针对性提示。
6. 通过后，本节写入本地完成列表，掌握度与检查点同步更新。
7. 用户可进入下一课，或切到项目研究/架构视图补充整体认识。
8. 手机端通过菜单按钮打开课程抽屉，其余流程保持一致。

## 5. 技术架构

```text
React components
    -> pure lesson data
    -> local exercise evaluator
    -> versioned localStorage adapter
    -> responsive CSS presentation
```

- 前端：React + Vite；组件状态足以承载当前规模，无需额外状态库。
- 图标：Lucide React，避免自绘图标与不一致的图形语言。
- 数据：课程内容静态打包，保证离线可用与来源可审查。
- 持久化：`hermes-learning-lab-progress-v1`，保存课程 ID 与最近位置。
- 安全：所有 Hermes 命令只渲染为字符串；应用没有 shell、文件系统或凭据访问。
- 部署：纯静态构建，可放在任意静态托管服务。

## 6. 视觉系统

- 参考用户截图的三栏深色工作台，但不复刻原产品。
- Canvas `#0B0B0A`，Surface `#141412/#1D1C19`，边界 `#34322C`。
- 琥珀色表示当前路径与主动操作，绿色表示完成/安全反馈，红色仅用于错误。
- 最大圆角 8px；页面区段采用开放布局，只有练习工具和重复项目使用卡片。
- 桌面固定左右轨道；低于 1180px 隐藏反馈栏；低于 820px 左栏变为抽屉。

## 7. 取舍与后续扩展

当前采用浏览器模拟而不连接真实 Hermes，换来零配置、可重复和不会污染用户环境。代价是无法验证真实 provider、Gateway 或 MCP 连接。

后续可增加：

- 后端 LMS 与账号同步
- 真实 Hermes API 的只读诊断模式
- 基于版本的课程内容更新
- 更丰富的排序题、命令纠错题和可下载结业报告
- 管理员课程编辑器与团队掌握度看板
