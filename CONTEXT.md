# 领域词汇

- **Learner（学习者）**：使用课程、模拟器和真实回执完成 Hermes 实验的人。
- **Local companion（本机伴随服务）**：运行在 `127.0.0.1` 的小型只读服务，只把脱敏状态交给课程网页。
- **Pairing（配对）**：网页输入终端显示的一次性配对码，换取持续到手动解除的浏览器令牌。
- **Detection request（检测请求）**：学习者点击检测后发送的单次本机状态读取请求；每次都要在终端确认。
- **Static mode（静态模式）**：GitHub Pages 等没有本机服务时的课程模式，保留模拟练习和回执验收。
- **Receipt verification（回执验收）**：学习者在 Desktop 或飞书中执行校验 Prompt，再在浏览器本地比对固定回执。
